<div id="menu_btn" class="header__toggle ss-icon attach_fastclick" on-click="toggle">
  {{#menuOpen}}
    left
  {{/menuOpen}}

  {{^menuOpen}}
    rows
  {{/menuOpen}}
</div>
<div class="header__balance">{{satoshiToBTC(bitcoinBalance)}}</div>
