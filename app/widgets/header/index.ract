<div id="menu_btn" class="header__toggle ss-icon attach_fastclick" on-click="toggle">
  {{#menuOpen}}
    left
  {{/menuOpen}}

  {{^menuOpen}}
    settings
  {{/menuOpen}}
</div>

<div class="header__balance">
  <span class="_amt">{{satoshiToBTC(bitcoinBalance)}}</span>
  <span class="_currency">BTC</span>
</div>

<div class='header__refresh ss-icon {{#updating_transactions}}is_updating{{/updating_transactions}}' on-click="sync">sync</div>