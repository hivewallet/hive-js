
<div class="search">

  {{^searching}}
    <div class='search__btn' on-click="search-nearby">{{search_msg}}</div>
  {{/searching}}

  {{#searching}}
    <div class='search__btn is_searching'>Searching...</div>
  {{/searching}}

  <div class="search__list">
    {{#nearbys}}
      <a href="#" class="search__item" on-click="select">
          <img class="_avatar" src="{{emailToAvatar(email)}}" />
          <h3 class="_name">{{name}}</span>
      </a>
    {{/nearbys}}
  </div>

</div>